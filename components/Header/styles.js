import { styled, theme } from "../../stitches.config";

export const Title = styled("h1", {
  fontFamily: "Messapia",
  fontSize: theme.fontSizes[5],
});

export const Subhead = styled("p", {
  fontFamily: "GT America Expanded Regular Trial",
  fontSize: theme.fontSizes[2],
});
